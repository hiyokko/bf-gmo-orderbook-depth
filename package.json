{
  "name": "bf-gmo-orderbook-depth",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "node scripts/run-orderbook-depth.mjs",
    "test": "node --test",
    "dry-run": "node scripts/run-orderbook-depth.mjs --dry-run",
    "watchdog": "node scripts/run-watchdog.mjs"
  },
  "engines": {
    "node": ">=24"
  }
}
